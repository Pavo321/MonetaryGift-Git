package com.mysteriousmonkeys.chanlo.event;

public enum ConfirmationType {
    AUTO,    // confirms automatically when all seats are filled (e.g. carpool)
    MANUAL   // host manually confirms after seeing interest (e.g. DJ night)
}
